### Localization
d-i debian-installer/locale string en_NZ.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i time/zone string Pacific/Auckland
d-i clock-setup/utc boolean true

### Network
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian13
d-i netcfg/get_domain string local

### Mirror
d-i apt-setup/disable-cdrom-entries boolean true
d-i apt-setup/use_mirror boolean true
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

## APT Setup
#d-i apt-setup/components multiselect main universe

### Users
d-i passwd/root-login boolean false

d-i passwd/user-fullname string Lab User
d-i passwd/username string user01
d-i passwd/user-password password changeme
d-i passwd/user-password-again password changeme

### Sudo setup
d-i passwd/user-default-groups string audio cdrom video sudo

### Disk partitioning (simple LVM, safe for homelab)
d-i partman-auto/disk string /dev/vda

d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max

d-i partman-auto/choose_recipe select atomic

d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite true

d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Packages
tasksel tasksel/first multiselect standard, ssh-server

d-i pkgsel/include string sudo qemu-guest-agent curl

d-i pkgsel/upgrade select full-upgrade

### GRUB (BIOS)
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string /dev/vda

### Late command (important for Packer)
d-i preseed/late_command string \
  in-target usermod -aG sudo user01
  in-target systemctl enable ssh; \
  in-target systemctl enable qemu-guest-agent; \
  in-target rm -f /etc/ssh/ssh_host_*; \
  in-target truncate -s 0 /etc/machine-id

### Finish
d-i finish-install/reboot_in_progress boolean false

